fn main() {
    println!("Hello, world!");
}

// Building and Running a Cargo Project

// make executable file: $ cargo build
// run executable file: $ ./target/debug/<sourceName>

// compile and run resulting executables:
// $ cargo run

// check code compiles but doesn't produce an executable
// $ cargo check

// building for release (compiles with optimisations but takes longer)
// $ cargo build --release